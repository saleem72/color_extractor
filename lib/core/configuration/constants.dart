//

import 'package:flutter/material.dart';

class AppConstants {
  AppConstants._internal();

  static const MaterialColor emptyMaterialColor = Colors.grey;
  static const Color emptyColor = Colors.grey;

  static BorderRadius borderRadius = BorderRadius.circular(12);
}
